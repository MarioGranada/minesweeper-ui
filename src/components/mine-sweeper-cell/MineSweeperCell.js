// import request from 'superagent';
// import Base from '../../services/base';

const methods = {
	updateCell() {

	}
}

const props = {
	status: {type: String},
	positionInArray: {type: Number},
	cell: {type: Object}
}

export default {
	props,
	methods
}
